//notación

// ; Sirve para delimitar el final de la linea
const b = 4;
b + 4;

// . Se utuliza en los objetos para acceder a los atributos
movil.anchura // Estoy accediendo a la propiedad/atributo (anchura) a traves del punto

// {} Se utiliza para definir objetos, declrar funciones y estructuras de control
const movil = {
    anchura: 5,
    altura: 10
}

if (true) {
    const constante2 = "hola"
}

// [] Se utilizan para las listas, arreglos y arrays
const ar = [1, 2, 3, 4]
console.log(ar[2])

// () Se utilizan para definir funciones
function suma (a, b)
